# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd propledger-nextjs && npm ci"]

[phases.build]
cmds = ["cd propledger-nextjs && npm run build"]

[start]
cmd = "cd propledger-nextjs && npm run start"
